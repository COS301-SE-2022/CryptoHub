import { render, screen } from '@testing-library/react';
import AdminFeed from '../AdminFeed';


describe("AdminFeed", () => {
    it('Should render test the admin feed somehow, please update these description', async () => {
        // render(<AdminFeed />);
        // const adminFeed = await screen.findByTestId('adminFeed');
        // expect(adminFeed).toBeInTheDocument();
    });
})